- [ ] Copy `src/assets/logo.ico` to `public/logo.ico` (so `public/index.html` can serve it)
- [x] Update `public/index.html` favicon link to `%PUBLIC_URL%/logo.ico`
- [ ] Run `npm start` and verify favicon in the browser tab
- [ ] (Optional) Hard-refresh / clear cache if favicon doesn’t update

